#include<iostream>
using namespace std;

int main(){
    vector <int> v1={2,4,6,8,10};
    cout<<"number of element: "<<v.size()<<endl;
    v.push_back(12);
    v.push_back(14);
    v.push_back(16);

    cout<<"number of element: "<<v.size();
    for(i:v)      
}